# UX Fixes Specification
_Generated by ux-critic.js on 2026-03-21T02:22:24.730Z_

## 01-start-screen.png

- [ ] Task: Increase contrast on "Associate PM" and "Account Executive" role labels — currently `text-gray-500` fails WCAG AA against zinc-900. Change to `text-gray-400` or `text-gray-300`.

- [ ] Task: Increase contrast on "L3 Software Engineer" breadcrumb text — currently `text-gray-500` is too dim. Change to `text-gray-400`.

- [ ] Task: Add visible focus states to all interactive elements (career track buttons, role selection cards). Currently no `:focus-ring` or `:focus-visible` classes present on selectable items.

- [ ] Task: Verify "LOCKED" badge contrast — `text-gray-500` on dark background may fail WCAG AA. Change to `text-gray-400` or add a lighter background.

- [ ] Task: Add hover state styling to career track cards and role options — currently no visual feedback on hover, reducing discoverability that items are clickable.

---

## 02-offer-letter.png

- [ ] Task: Increase contrast of italicized fine-print text ("payable bi-weekly, net of dreams", "4-yr cliff, subject to re-org", etc.). Currently uses light gray that may fail WCAG AA against zinc-900. Change from `text-gray-400` to `text-gray-300` or add `font-medium`.

- [ ] Task: Add focus/active states to the implied accept/decline action buttons. No visible button elements exist in this screenshot, but the closing paragraph ("By accepting this offer...") suggests interactive elements should be present. Ensure any buttons have `focus:outline-none focus:ring-2 focus:ring-orange-500` or similar.

- [ ] Task: Verify the left border accent (orange vertical line) has sufficient contrast. If it's `border-orange-500`, confirm it meets WCAG AAA for a 3px+ line. If dimmer, brighten to `border-orange-400`.

- [ ] Task: Check that the monospace font rendering (entire offer letter) is crisp at smaller sizes. If using `font-mono` with `text-sm`, consider `text-base` for readability, or ensure anti-aliasing is enabled (`antialiased` class).

---

## 03-l3-dashboard.png

- [ ] Task: "Write integration..." text in IN PROGRESS card is truncated. Add `line-clamp-2` or increase card width, or use `truncate` with tooltip.
- [ ] Task: "Update OpenAI-spec for v2 endpoints" text in DONE card is struck-through and barely visible. Increase contrast or reconsider strikethrough styling on zinc-900 background.
- [ ] Task: "grind cost 3 BW/click" label in IN PROGRESS card has low contrast (appears dim gray on dark background). Change text color to `text-zinc-300` or `text-zinc-200`.
- [ ] Task: Recovery actions section shows "Coffee [+25BW]" and "Consult [+20]" — the bracketed values are unclear without context. Add aria-labels or hover tooltips explaining what BW and the numeric notation mean.
- [ ] Task: "Grind [-3 BW]" button in IN PROGRESS card lacks visible focus/active state. Add focus ring with `focus:ring-2 focus:ring-blue-500` or similar.
- [ ] Task: All "Work →" and "Escalate" buttons lack visible hover states. Add `hover:bg-opacity-80` or `hover:brightness-125` for interactive feedback.

---

## 04-l5-dashboard-junior-devs.png

- [ ] Task: "Review Architecture..." text in IN PROGRESS card is truncated. Add `line-clamp-2` or increase card width, or use `text-sm` to fit content within the card bounds.

- [ ] Task: "Align cross-team on API..." text in IN REVIEW card is truncated. Apply `line-clamp-2` or increase card width to prevent cutoff.

- [ ] Task: The orange progress bar in "Review Architecture..." card has low contrast against the dark zinc background. Increase bar opacity or use a brighter orange (`from-orange-400` instead of current shade) to meet WCAG AA standards.

- [ ] Task: Rush buttons ([+5D], [+8D], [+6D]) lack visible focus/active states. Add `focus:ring-2 focus:ring-orange-500` and `active:scale-95` for keyboard navigation and click feedback.

- [ ] Task: Escalate buttons lack hover states. Add `hover:bg-zinc-700 hover:border-zinc-600` to all escalate button variants for visual feedback.

- [ ] Task: "awaiting review" label (14s) in IN REVIEW card has low contrast with zinc-900 background. Change to `text-orange-400` or `text-zinc-300` to improve readability.

- [ ] Task: "empty" placeholder text in DONE column is very faint. Increase contrast by changing from `text-zinc-600` to `text-zinc-500` or higher.

---

## 05-chaos-scope-creep.png

- [ ] Task: Increase contrast of blue hyperlink text in the modal dialog. Change from current light blue to `text-blue-300` or `text-blue-200` to meet WCAG AA standards against the dark brown/black modal background.

- [ ] Task: Add visible focus state to the "Acknowledge →" button. Currently missing keyboard focus indicator. Add `focus:outline-2 focus:outline-offset-2 focus:outline-amber-500` to the button element.

- [ ] Task: Verify the modal backdrop opacity. The dark overlay appears very dark — ensure it's not obscuring the background interface too aggressively. Consider adjusting backdrop to `bg-black/60` if currently higher.

- [ ] Task: Check text contrast of the "RANDOM EVENT" label and "Scope Creep" heading against the dark brown modal background. If failing WCAG AA, brighten to `text-amber-100` or `text-orange-100`.

- [ ] Task: Ensure the pixelated flame icon has adequate padding/margin to prevent accidental clickthrough to background elements. Verify the modal has proper `pointer-events-auto` applied.

---

## 06-chaos-pagerduty.png

- [ ] Task: Increase contrast of blue hyperlinked text in the central panel (Sample Data, Sample Output sections). Change from current blue to `text-blue-300` or `text-blue-200` to meet WCAG AA standards against the zinc-900 background.

- [ ] Task: Add focus state to the "Acknowledge →" button in the PagerDuty Alert modal. Currently missing visible `:focus-visible` ring. Add `focus:ring-2 focus:ring-orange-400` or equivalent.

- [ ] Task: Verify the "-15 Bandwidth" text in the alert modal has sufficient contrast. The red text appears to be `text-red-500` which may be borderline against the dark red/maroon modal background. Consider `text-red-300` for better readability.

- [ ] Task: Check if the central content panel has a vertical scrollbar that should be hidden or styled. The "Sample Data" and other text sections appear to have constrained height but scrollbar visibility is unclear due to image quality.

- [ ] Task: Add hover state styling to the "Acknowledge →" button. Currently no visible hover feedback defined. Add `hover:bg-orange-600` or `hover:opacity-80`.

---

## 07-chaos-flow-state.png

- [ ] Task: Increase contrast of light gray text in the modal content area. Change text color from current light gray to `text-gray-100` or `text-white` to meet WCAG AA standards against the `bg-zinc-900` background.

- [ ] Task: Add focus state styling to the "Acknowledge →" button. Include `:focus-visible:ring-2 :focus-visible:ring-offset-2 :focus-visible:ring-cyan-500` for keyboard navigation accessibility.

- [ ] Task: Verify hover state exists on "Acknowledge →" button. Add `hover:bg-zinc-700 transition-colors` or similar visual feedback.

- [ ] Task: Review the modal border color (`border-cyan-600` appears correct, but verify it meets contrast requirements against the dark background for users with color blindness).

---

## 08-chaos-mandatory-meeting.png

- [ ] Task: Increase contrast of blue hyperlinked text in the modal (currently appears as low-contrast blue on dark background). Change from `text-blue-400` or similar to `text-blue-300` or add `font-semibold` for better WCAG AA compliance.

- [ ] Task: Add visible focus state to "Acknowledge →" button. Currently missing `:focus-visible` ring styling. Add `focus-visible:ring-2 focus-visible:ring-orange-500 focus-visible:ring-offset-2 focus-visible:ring-offset-zinc-900`.

- [ ] Task: Verify modal backdrop opacity. The overlay appears very dark and may obscure background content excessively. Consider adjusting `bg-black/50` or `bg-black/40` for better visibility of game state behind the modal.

- [ ] Task: Check "-15 Bandwidth" warning text contrast at bottom of modal. The red text on dark background may not meet WCAG AA standards. Verify it's `text-red-400` or brighter, or add `font-semibold`.

---

## 09-chaos-optional-outing.png

- [ ] Task: Increase contrast of the "RANDOM EVENT" label text — change from current light gray to `text-yellow-400` or `text-amber-300` to meet WCAG AA standards against the zinc-900 modal background.

- [ ] Task: Increase contrast of the event description text "Happy hour at the rooftop bar. You went anyway." — change from current light gray to `text-gray-100` or `text-white` to improve readability against zinc-900 background.

- [ ] Task: Increase contrast of the reward/penalty text "-10 Bandwidth, +5% Manager's Good Books." — change from current light gray to `text-gray-100` to meet accessibility standards.

- [ ] Task: Add explicit focus state styling to the "Acknowledge →" button — ensure it has a visible `:focus-visible` ring (e.g., `focus:ring-2 focus:ring-yellow-400`) for keyboard navigation accessibility.

- [ ] Task: Verify the "Acknowledge →" button has sufficient padding and touch target size (minimum 44x44px) — adjust `px` and `py` values if needed for mobile accessibility.

---

## 10-chaos-pm-status-update.png

- [ ] Task: Increase contrast on "PM Status Update" heading text — currently appears as light gray on dark brown modal background. Change text color from current shade to `text-yellow-400` or `text-white` to meet WCAG AA standards.

- [ ] Task: Increase contrast on body text in modal ("Can you just update the tracker real quick?" and "- 45 min later.") — apply `text-gray-100` or `text-white` instead of current light gray to improve readability against `bg-zinc-950/95` backdrop.

- [ ] Task: Add visible focus state to "Acknowledge →" button — currently missing `:focus-visible` ring. Add `focus-visible:ring-2 focus-visible:ring-orange-400 focus-visible:ring-offset-2 focus-visible:ring-offset-zinc-950`.

- [ ] Task: Verify modal backdrop z-index — ensure `z-50` or higher is applied to the overlay div containing the orange-bordered modal to guarantee it layers above all sidebar and main content elements.

- [ ] Task: Add hover state styling to "Acknowledge →" button — currently missing visual feedback. Apply `hover:bg-orange-400 hover:text-zinc-950 transition-colors` or similar to indicate interactivity.

---

## 11-chaos-re-org.png

- [ ] Task: Increase contrast of blue hyperlinked text in the modal body ("Sample Task", "In Progress", "Story point") — current appears to be `text-blue-400` or similar on dark background. Change to `text-blue-300` or add `font-semibold` for better WCAG AA compliance.

- [ ] Task: Add focus ring to "Acknowledge →" button — currently missing `focus:ring-2 focus:ring-offset-2 focus:ring-offset-zinc-900` or equivalent to meet keyboard navigation standards.

- [ ] Task: Verify modal backdrop opacity — the purple border modal appears to have low contrast against the dark background. Ensure backdrop is `bg-black/50` or darker for better visual separation.

- [ ] Task: Check text contrast on "RANDOM EVENT" and "Re-Org Announcement" header text — ensure `text-white` or `text-zinc-50` is used (not gray variants) for critical heading hierarchy.

---

## 12-chaos-standup.png

- [ ] Task: The "Acknowledge →" button needs a visible focus state. Add `focus:outline-2 focus:outline-offset-2 focus:outline-orange-500` to ensure keyboard navigation visibility.

- [ ] Task: The modal backdrop appears to have insufficient contrast between the overlay and the dark background. Consider increasing the backdrop opacity from current level to `bg-black/70` or similar for better modal distinction.

- [ ] Task: The "-10 Bandwidth." penalty text at bottom left uses a color that may have insufficient contrast against the zinc-900 background. Verify it meets WCAG AA standards or increase brightness of the text color.

- [ ] Task: The "Quick Sync?" title and body text in the modal could benefit from improved line-height. Add `leading-relaxed` to the text container for better readability of the longer message.

---

## 13-chatty-manager.png

- [ ] Task: Increase contrast on "Action points remaining this sprint" text — change from current gray to `text-zinc-100` or `text-gray-100` to meet WCAG AA standards against zinc-900 background
- [ ] Task: Increase contrast on "Recovery actions:" label — apply `text-zinc-100` instead of current lighter gray for better readability
- [ ] Task: Add missing focus state to "Grind [-3 BW]" button — add `focus:outline-none focus:ring-2 focus:ring-orange-500` for keyboard navigation accessibility
- [ ] Task: Add missing focus state to "Escalate [-15A/-15M]" button — add `focus:outline-none focus:ring-2 focus:ring-orange-500` for keyboard navigation accessibility
- [ ] Task: Increase contrast on column headers in Sprint Board ("TO DO", "IN PROGRESS", "IN REVIEW", "DONE") — change from current gray to `text-zinc-200` or higher
- [ ] Task: Add missing hover state to "Sample Task" card — implement `hover:border-orange-500 hover:shadow-lg` transition for interactive feedback
- [ ] Task: Increase contrast on "progress" and "grind cost 3 BW/click" text in task card — change to `text-zinc-100` for readability

---

## 14-burnout-crash-modal.png

- [ ] Task: Increase contrast of "empty" labels in TO DO and DONE columns (currently light gray text on dark background). Change text color from current gray to `text-zinc-300` or `text-zinc-200`.

- [ ] Task: Increase contrast of "Progress" label under "Sample Task" card. Change from current light gray to `text-zinc-300` or higher.

- [ ] Task: Verify the "Accept & Continue →" button has visible focus state. Add `focus:ring-2 focus:ring-red-500 focus:outline-none` if missing.

- [ ] Task: Check contrast of left sidebar metric labels (e.g., "XP: 800", "$25,000", "BANDWIDTH", "BURNOUT", etc.). If text is `text-zinc-500` or lighter, upgrade to `text-zinc-400` or `text-zinc-300`.

- [ ] Task: Verify the red modal border and button have sufficient contrast against the dark backdrop. Ensure button text "Accept & Continue →" meets WCAG AA at 14px+ (currently appears to pass, but confirm final render).

- [ ] Task: Check the "50% Bandwidth" text inside the modal—if it's rendered as plain text without sufficient weight or color emphasis, ensure it uses `text-red-400` or `font-semibold` for clarity.

---

## 15-consult-modal.png

- [ ] Task: Increase contrast on "FREE TIER" label — change from current gray to `text-zinc-300` or `text-zinc-200` to meet WCAG AA standards against zinc-900 background
- [ ] Task: Increase contrast on stat modifiers (+20, -10, -5) — change from current orange/amber to `text-amber-400` or `text-yellow-300` for better readability
- [ ] Task: Increase contrast on "Instantly completes oldest in-progress task" and "-$3,000 Net Worth" text — change from current cyan/green to `text-emerald-300` or `text-cyan-300` for WCAG AA compliance
- [ ] Task: Add visible focus state to "Take Free Consult →" button — currently appears to lack `:focus-visible:ring` styling for keyboard navigation accessibility
- [ ] Task: Add visible focus state to "Hire Staff Eng [$3,000] →" button — currently appears to lack `:focus-visible:ring` styling for keyboard navigation accessibility
- [ ] Task: Verify modal backdrop opacity — ensure `bg-black/50` or similar is applied to the overlay behind the dialog to prevent content bleed-through on lower contrast displays

---

## 16-cycle-summary-loot-drop.png

- [ ] Task: Increase contrast on "BANDWIDTH RESTORED" label — change text color from current gray to `text-gray-300` or `text-gray-200` to meet WCAG AA standards against zinc-900 background
- [ ] Task: Increase contrast on "FACTION STANDING" section labels (PRODUCT, ENGINEERING, SALES) — change from current gray to `text-gray-300` to improve readability
- [ ] Task: Increase contrast on "end of sprint summary" subtitle — change from current dim gray to `text-gray-400` for better visibility
- [ ] Task: Add visible focus/hover states to perk buttons ("Caffeine Addict", "Blame Shifter", "Rubber Duck") — currently have no interactive affordance; add `hover:bg-zinc-700` or `hover:border-cyan-400` and `focus:ring-2 focus:ring-cyan-400`
- [ ] Task: Verify "REWARD DROP" section heading contrast — change to `text-gray-300` if currently below WCAG AA threshold

---

## 17-shop-perks-tab.png

- [ ] Task: Increase contrast on "PERKS & TOOLS" and "COSMETICS" tab labels — currently light gray on dark zinc. Change from `text-gray-400` to `text-gray-200` or `text-gray-100`.

- [ ] Task: Increase contrast on "RECOVERY" section header — change from `text-gray-400` to `text-gray-200`.

- [ ] Task: Increase contrast on "TOOL CHEST" section header — change from `text-gray-400` to `text-gray-200`.

- [ ] Task: Increase contrast on item descriptions (e.g., "Take a day off. Reduces Burnout by 15%") — currently `text-gray-400`, should be `text-gray-300` or `text-gray-200`.

- [ ] Task: Ensure button hover states are visible on all shop items (Mental Health Day, Hawaii Vacation, etc.) — add `:hover:bg-zinc-700` or `:hover:border-gray-300` to the item containers.

- [ ] Task: Add focus ring styling to all interactive buttons (price buttons and tab controls) — add `focus:outline-none focus:ring-2 focus:ring-orange-500` for keyboard navigation accessibility.

- [ ] Task: Verify the "Blocks 1 event" label under Noise Canceling Headphones is not truncated — check container width and add `whitespace-nowrap` or adjust layout if needed.

---

## 18-shop-cosmetics-tab.png

- [ ] Task: Increase contrast on "UI THEMES" section label — change from `text-gray-500` to `text-gray-400` to meet WCAG AA for small text on zinc-900 backgrounds
- [ ] Task: Increase contrast on "AVATARS" section label — change from `text-gray-500` to `text-gray-400` for consistency and readability
- [ ] Task: Increase contrast on cosmetics tab and perks & tools tab labels — change from `text-gray-500` to `text-gray-400` to improve tab discoverability
- [ ] Task: Add visible focus state to "Hacker Green" purchase button — add `focus:ring-2 focus:ring-blue-500` to match active state styling on other buttons
- [ ] Task: Add visible focus state to "Vaporwave" purchase button — add `focus:ring-2 focus:ring-blue-500` for keyboard navigation accessibility
- [ ] Task: Add visible focus state to "Code Ninja" purchase button — add `focus:ring-2 focus:ring-blue-500` for keyboard navigation accessibility
- [ ] Task: Verify "Robot Mode" button at bottom is fully visible — add scrollbar styling or adjust modal max-height if content is being clipped below viewport fold

---

## 19-hacker-green-theme.png

- [ ] Task: Increase contrast on "empty" placeholder text in TO DO, IN REVIEW, and DONE columns. Change from current gray to `text-zinc-400` or `text-zinc-300` for WCAG AA compliance against `bg-zinc-900`.

- [ ] Task: Add visible focus state to "Grind [-3 BW]" button. Currently has no outline or ring on focus. Add `focus:ring-2 focus:ring-orange-500` or similar.

- [ ] Task: Add visible focus state to "Escalate [-15A/-15M]" button. Currently has no outline or ring on focus. Add `focus:ring-2 focus:ring-orange-500` or similar.

- [ ] Task: Verify hover state on "Sample Task" card. Ensure there's a visual feedback mechanism (border glow, shadow, or background shift) on `:hover`.

- [ ] Task: Check contrast on "IN PROGRESS" badge text inside the Sample Task card. If it's light gray on blue background, verify it meets WCAG AA standards (4.5:1 minimum for small text).

- [ ] Task: Verify "grind cost 3 BW/click" text contrast against the dark blue card background. If it's `text-zinc-400`, it may fail WCAG AA. Consider `text-zinc-300` or lighter.

---

## 20-extreme-stats-stress-test.png

- [ ] Task: Increase contrast on "empty progress" text in TO DO column — change from current gray to `text-zinc-300` or `text-zinc-200` to meet WCAG AA standards against zinc-900 background
- [ ] Task: Increase contrast on "empty" text in IN REVIEW and DONE columns — apply `text-zinc-300` instead of current lighter gray
- [ ] Task: Add focus state to "Grind [-3 BW]" button — missing visible `:focus-visible` ring (recommend `focus:ring-2 focus:ring-blue-500`)
- [ ] Task: Add focus state to "Escalate [-15A/-15M]" button — missing visible `:focus-visible` ring (recommend `focus:ring-2 focus:ring-blue-500`)
- [ ] Task: Increase contrast on "grind cost 3 BW/click" and "reward 10 XP" labels — change to `text-zinc-300` or higher to meet WCAG AA
- [ ] Task: Add hover state to "Coffee [+25BW]" recovery action button — no visible feedback on interaction
- [ ] Task: Add hover state to "Consult [+20]" recovery action button — no visible feedback on interaction

---

## 21-performance-review-promotion.png

- [ ] Task: Increase contrast on "MANAGER" label and manager feedback text. Change text color from current light gray to `text-gray-100` or `text-white` to meet WCAG AA standards against zinc-900 background.

- [ ] Task: Add visible focus state to the "Thank you. I'm ready." button. Currently has no outline or ring style; add `focus:ring-2 focus:ring-green-500 focus:outline-none` for keyboard navigation accessibility.

- [ ] Task: Add hover state styling to the "Thank you. I'm ready." button. Currently appears static; add `hover:bg-zinc-700 hover:border-gray-400` transition for interactive feedback.

- [ ] Task: Verify the "PROMOTED" badge has sufficient contrast. If text is dark on green background, ensure it meets WCAG AA by checking luminosity ratio or adjusting to `bg-green-600` with `text-white`.

- [ ] Task: Add `transition-all duration-200` to button element to smooth hover/focus state changes and improve perceived interactivity.

---

## 22-performance-review-pip.png

- [ ] Task: Increase contrast on "MANAGER" label and manager feedback text. Change text color from current gray to `text-zinc-100` or `text-white` to meet WCAG AA standards against zinc-900 background.

- [ ] Task: Add hover state to "I understand the feedback." button. Currently missing `:hover` styling; add `hover:bg-zinc-700 hover:border-zinc-600 transition-colors` for visual feedback.

- [ ] Task: Add focus state to "I understand the feedback." button for keyboard navigation accessibility. Include `focus:outline-none focus:ring-2 focus:ring-blue-500`.

- [ ] Task: Verify "PIP" badge contrast. The red background appears low contrast; ensure it meets WCAG AA by checking against actual color values or adjusting to `bg-red-600` if currently lighter.

- [ ] Task: Add visual distinction to metric "fail" states. The red text on dark zinc for IMPACT, AUTONOMY, and PROJECT DEBT labels is borderline; verify contrast ratio meets WCAG AA or increase saturation.

---

## 23-game-over.png

- [ ] Task: Increase contrast of the body text "The committee has decided..." by changing from current gray to `text-gray-200` or `text-gray-100` to meet WCAG AA standards against the `bg-zinc-900` background.

- [ ] Task: Add visible focus state to the "Try Again →" button by adding `focus:outline focus:outline-2 focus:outline-offset-2 focus:outline-red-500` for keyboard accessibility.

- [ ] Task: Add hover state to the "Try Again →" button with `hover:bg-red-700` or `hover:border-red-400` to provide visual feedback.

- [ ] Task: Verify the red text "PERFORMANCE IMPROVEMENT PLAN" has sufficient contrast; consider changing to `text-red-400` if current red is closer to `text-red-600`.

---

## 24-job-board.png

- [ ] Task: Increase contrast on "Free" labels in TIER 1 and TIER 2 cards. Change from `text-gray-400` to `text-gray-300` or `text-gray-200` to meet WCAG AA standards for body text on zinc-900 backgrounds.

- [ ] Task: Increase contrast on descriptive text in all three tier cards (starting with "Scrappy Startup Inc..." etc.). Change from `text-gray-400` to `text-gray-300` to improve readability against zinc-900.

- [ ] Task: Add explicit focus states to all "Join – Free →" buttons. Currently missing `:focus-visible:ring-2 :focus-visible:ring-offset-2` classes for keyboard navigation accessibility.

- [ ] Task: Add hover state styling to "Join – Free →" buttons. Buttons appear static; add `hover:opacity-80` or `hover:bg-opacity-90` to indicate interactivity.

- [ ] Task: Verify "PR Review Time: 15s (slow!)" text in TIER 3 doesn't truncate on smaller viewports. Consider adding `break-words` or adjusting container width if needed.

- [ ] Task: Add hover state to "Alumni Network →" button at bottom. Currently missing visual feedback for interactivity; add `hover:opacity-80` or similar.

- [ ] Task: Increase contrast on "Max BW:" and "PR Review Time:" labels in all cards. Change from `text-gray-400` to `text-gray-300` for better readability.

---

## 25-alumni-network.png

- [ ] Task: Increase contrast for "META-PROGRESSION" label — change from current gray to `text-zinc-300` or `text-zinc-200` to meet WCAG AA standards against zinc-900 background
- [ ] Task: Increase contrast for descriptive text "Invest your career earnings..." — change from current gray to `text-zinc-300` to improve readability
- [ ] Task: Increase contrast for cost labels (orange text "$50,000", "$30,000", "$20,000") — verify they meet WCAG AA; if not, brighten to `text-orange-400`
- [ ] Task: Add visible focus state to all "Buy" buttons — currently missing `:focus-visible:ring` or `:focus-visible:outline` for keyboard accessibility
- [ ] Task: Add hover state visual feedback to "Buy" buttons — implement `:hover:bg-orange-600` or similar to indicate interactivity
- [ ] Task: Verify "Career Net Worth" value "$80,000" has sufficient contrast — may need `text-yellow-400` or `text-amber-300` instead of current color
- [ ] Task: Add focus state to "← Back to Job Board" button — missing `:focus-visible` ring/outline for keyboard navigation
- [ ] Task: Increase contrast for item descriptions ("Start new jobs at L4...", "Burnout caps at 90%...", "Severance grants...") — change to `text-zinc-300` for better readability against zinc-900

---

## 26-pro-unlock-modal.png

- [ ] Task: Increase contrast of the descriptive text "You've proven yourself at L6..." and "One-time purchase..." by changing from current gray to `text-gray-100` or `text-white` to meet WCAG AA standards against the zinc-900 background.

- [ ] Task: Add visible focus state to "Buy Full Game – $4.99 →" button with `focus:ring-2 focus:ring-orange-400 focus:outline-none` for keyboard navigation accessibility.

- [ ] Task: Increase contrast of the dev-mode note at bottom "(Dev mode: clicking above...)" by changing from current dim gray to at least `text-gray-400` to improve readability.

- [ ] Task: Add hover state to "Buy Full Game – $4.99 →" button with `hover:bg-orange-700 hover:shadow-lg transition-all` to provide visual feedback.

- [ ] Task: Verify the checkmark items (✓ L5 → L7..., ✓ Staff, Director..., ✓ ALL future...) have sufficient contrast; consider changing text color to `text-gray-100` if currently using lower contrast gray.

---

